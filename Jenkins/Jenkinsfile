pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo "Checking out branch feature/jenkins"
                checkout scm
            }
        }

        stage('List Files') {
            steps {
                sh 'ls -la'
            }
        }
    }
}